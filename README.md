# GlobalScale Currency Utility
A Python-based currency converter featuring a Tkinter GUI. 
This project demonstrates version control hygiene and AI-collaboration.

## Setup
1. Ensure Python 3.11+ is installed.
2. Run `python gui.py` to start the application.